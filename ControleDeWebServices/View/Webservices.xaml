<Page x:Class="ControleDeWebServices.View.WebServices"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:diversos="clr-namespace:ControleDeWebServices.Diversos"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="650"
      d:DesignWidth="850"
      Title="WebServices">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <diversos:EnumConverter x:Key="GeneralEnumConverter"/>
        <Style x:Key="PrimaryActionButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Background" Value="#2F6F8F"/>
            <Setter Property="BorderBrush" Value="#2F6F8F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>
        <Style x:Key="SuccessActionButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Background" Value="#4E9A57"/>
            <Setter Property="BorderBrush" Value="#4E9A57"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>
        <Style x:Key="GridHeaderStyle" TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
            <Setter Property="Background" Value="#2F6F8F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>
    </Page.Resources>

    <Grid Background="#F7F9FA">
        <Border Margin="18" Padding="18" Background="#FCFDFD" BorderBrush="#D7E0E4" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="1.5*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="WebServices" FontSize="22" FontWeight="SemiBold" Foreground="#172126"/>
                        <TextBlock Text="Consulta, atualização de URL e execução operacional" Margin="0,3,0,0" Foreground="#5C6B73"/>
                    </StackPanel>
                    <TextBlock Grid.Column="1" Text="{Binding ExecutionStatus}" Foreground="#5C6B73" VerticalAlignment="Center"/>
                </Grid>

                <GroupBox Grid.Row="1" Margin="0,16,0,0" Header="10 mais acessados">
                    <DataGrid ItemsSource="{Binding MaisAcessados}" SelectedItem="{Binding SelectedMaisAcessado, Mode=TwoWay}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" HeadersVisibility="Column" Background="#FCFDFD" AlternatingRowBackground="#F7F9FA" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#D7E0E4" materialDesign:DataGridAssist.CellPadding="10,7,10,7">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Código" Width="80" Binding="{Binding CodigoControle}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTextColumn Header="Cliente" Width="*" Binding="{Binding NomeCliente}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTextColumn Header="Sistema" Width="*" Binding="{Binding NomeSistema}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTextColumn Header="UF" Width="55" Binding="{Binding Uf}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTextColumn Header="Tipo de Conexão" Width="130" Binding="{Binding TipoConexao, Converter={StaticResource GeneralEnumConverter}}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTextColumn Header="Acessos" Width="80" Binding="{Binding QuantidadeAcessos}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTemplateColumn Header="Ações" Width="180" HeaderStyle="{StaticResource GridHeaderStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource SuccessActionButton}" Command="{Binding DataContext.ExecutarSelecionadoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="Executar"/>
                                            <Button Margin="6,0,0,0" Style="{StaticResource PrimaryActionButton}" Command="{Binding DataContext.AtualizarUrlCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="URL"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>

                <Border Grid.Row="2" Margin="0,16,0,14" Padding="12" Background="#F7F9FA" BorderBrush="#D7E0E4" BorderThickness="1" CornerRadius="4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox materialDesign:HintAssist.Hint="Código" Text="{Binding CodigoCliente, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                        <ComboBox Grid.Column="1" materialDesign:HintAssist.Hint="UF" ItemsSource="{Binding Ufs}" SelectedItem="{Binding SelectedUf, Mode=TwoWay}" Margin="0,0,10,0"/>
                        <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="Cliente" ItemsSource="{Binding Clientes}" SelectedItem="{Binding SelectedCliente, Mode=TwoWay}" DisplayMemberPath="NomeCliente" Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="3" Orientation="Horizontal">
                            <Button Style="{StaticResource SuccessActionButton}" Command="{Binding ExecutarSelecionadoCommand}" Content="Executar selecionado"/>
                            <Button Margin="8,0,0,0" Style="{StaticResource PrimaryActionButton}" Command="{Binding AtualizarUrlCommand}" Content="Atualizar URL"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <GroupBox Grid.Row="3" Header="Resultado da consulta">
                    <DataGrid ItemsSource="{Binding Sistemas}" SelectedItem="{Binding SelectedSistema, Mode=TwoWay}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" HeadersVisibility="Column" Background="#FCFDFD" AlternatingRowBackground="#F7F9FA" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#D7E0E4" materialDesign:DataGridAssist.CellPadding="10,7,10,7">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Código" Width="80" Binding="{Binding CodigoControle}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTextColumn Header="Cliente" Width="*" Binding="{Binding NomeCliente}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTextColumn Header="Sistema" Width="*" Binding="{Binding NomeSistema}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTextColumn Header="UF" Width="55" Binding="{Binding Uf}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTextColumn Header="Tipo de Conexão" Width="130" Binding="{Binding TipoConexao, Converter={StaticResource GeneralEnumConverter}}" HeaderStyle="{StaticResource GridHeaderStyle}"/>
                            <DataGridTemplateColumn Header="Ações" Width="180" HeaderStyle="{StaticResource GridHeaderStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource SuccessActionButton}" Command="{Binding DataContext.ExecutarSelecionadoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="Executar"/>
                                            <Button Margin="6,0,0,0" Style="{StaticResource PrimaryActionButton}" Command="{Binding DataContext.AtualizarUrlCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Content="URL"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>

                <Grid Grid.Row="4" Margin="0,12,0,0">
                    <TextBlock Text="{Binding ExecutionStatus}" Foreground="#5C6B73"/>
                    <ItemsControl ItemsSource="{Binding ExecutionSteps}" HorizontalAlignment="Right">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="4,0,0,0" Padding="8,3" Background="#EAF2F5" BorderBrush="#D7E0E4" BorderThickness="1" CornerRadius="3">
                                    <TextBlock Text="{Binding}" Foreground="#2F6F8F" FontSize="11"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>
