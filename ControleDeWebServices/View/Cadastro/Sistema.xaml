<Page x:Class="ControleDeWebServices.View.Cadastro.Sistema"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="220"
      d:DesignWidth="850"
      Title="Sistema">

    <Grid Background="{StaticResource App.Brush.Background}">
        <Border Style="{StaticResource App.PageSurface}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Titulo}" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource App.Brush.TextPrimary}"/>
                <Grid Grid.Row="1" Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="90"/>
                    </Grid.ColumnDefinitions>
                    <TextBox materialDesign:HintAssist.Hint="Nome do sistema" Text="{Binding NomeSistema, UpdateSourceTrigger=PropertyChanged}" CharacterCasing="Upper" Margin="0,0,10,0"/>
                    <ComboBox Grid.Column="1" materialDesign:HintAssist.Hint="UF" ItemsSource="{Binding Estados}" SelectedItem="{Binding Uf, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>
