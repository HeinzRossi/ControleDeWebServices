<Page x:Class="ControleDeWebServices.View.Cadastro.Secoes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="220"
      d:DesignWidth="850"
      Title="Seção">

    <Grid Background="{StaticResource App.Brush.Background}">
        <Border Style="{StaticResource App.PageSurface}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Titulo}" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource App.Brush.TextPrimary}"/>
                <TextBox Grid.Row="1" Margin="0,14,0,0" materialDesign:HintAssist.Hint="Nome da seção" Text="{Binding NomeSecao, UpdateSourceTrigger=PropertyChanged}" CharacterCasing="Upper"/>
            </Grid>
        </Border>
    </Grid>
</Page>
